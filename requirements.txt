fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
python-multipart==0.0.9
python-dotenv==1.0.1
sentence-transformers==2.7.0
faiss-cpu
google-generativeai>=0.5.0
requests==2.32.2
numpy==1.26.4
# Optional: pip install cohere  (for reranking)
# Note: Groq uses the REST API directly via requests — no extra SDK needed.
